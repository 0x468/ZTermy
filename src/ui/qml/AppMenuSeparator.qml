import QtQuick
import QtQuick.Controls

MenuSeparator {
    horizontalPadding: 8
    verticalPadding: 5

    contentItem: Rectangle {
        implicitWidth: 172
        implicitHeight: 1
        color: Theme.border
    }
}
