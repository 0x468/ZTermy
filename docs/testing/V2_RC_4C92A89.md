# V2 implementation candidate: 4c92a89

Status: superseded by the accepted `8f8fccf` candidate after V2 repair work

The immutable record for the released personal/private V2 baseline is
[V2_RC_8F8FCCF.md](V2_RC_8F8FCCF.md). The hashes below remain historical and
must not be used as `v0.2.0` release artifacts.

## Candidate identity

| Field | Value |
| --- | --- |
| Date | 2026-08-02 |
| Source commit | `4c92a89d68591473b4297417bb0a4c3380b10102` |
| Product milestone | V2 stable personal native SSH workspace |
| Version | `0.2.0` |
| Platform | Windows x64 |
| Release bundle | `build/msvc-static-release/package/release/ztermy-0.2.0-windows-x64` |

The documentation commit containing this record is not part of the binaries.
The artifact hashes below identify the exact executable source commit shown in
the table.

## Automated evidence

The static Release `ztermy_v2_automated_preflight` completed successfully:

- all 87 C++ translation units passed clang-tidy with diagnostics treated as
  errors;
- C++ formatting, all 33 QML formatting checks, qmllint, and all 710 finished
  Simplified Chinese translations passed;
- all seven real-window gates passed, including work-area maximize, materials,
  resize, keyboard layout, terminal rendering, and synthetic 100%, 125%, 150%,
  and 200% DPI;
- all 37 non-real-host CTest tests passed; `ssh-real-host` was excluded by exact
  name and no network host was contacted;
- the QML/native-window smoke test entered the real Qt event loop and completed
  controller shutdown and resource release;
- the portable ZIP, per-user MSI, installer contract, release manifest, and
  checksum manifest were rebuilt from the candidate;
- WiX validation produced only the reviewed ICE61, ICE69, and ICE91 warnings
  documented in ADR 0011.

The same source commit then completed a dynamic Debug build, all 37
non-real-host tests, and the same seven real-window gates. The PE metadata test
passed in both configurations and reported the version derived from `0.2.0`.

An earlier preflight correctly failed on static-only clang-tidy diagnostics.
The candidate includes the equivalent cleanup in `4c92a89`; focused dynamic
and static AppController, credential-vault, and transfer-manager tests passed
before the successful full rerun. Failed intermediate package hashes are not
candidate artifacts.

## Artifact integrity

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `ztermy-0.2.0-windows-x64-portable.zip` | 18,602,880 | `1c6f0e089baf06fa1d261a04a2c5813cd4df5ddeed9200616d703bda72d53613` |
| `ztermy-0.2.0-windows-x64.msi` | 15,347,712 | `0a79ef51619b411f7474d1738c511018eebdd80a6d0f328fca0d2b5b78e47f14` |

`SHA256SUMS.txt` and `release-manifest.json` contain those exact values. The
handoff directory contains exactly the two artifacts and two manifests.

## Product contract represented by the candidate

- local and SSH terminal foundations, Unicode/IME/selection/clipboard,
  alternate screen, search, resize, and scrollback;
- saved hosts, trust, Windows and portable credentials, and locked-vault UX;
- terminal-attached SFTP, background transfers, conflict handling, retry,
  cancellation, and crash recovery;
- actions, command palette, editable shortcuts, history, scripts, composer,
  session logging, diagnostics, and safe workspace restoration;
- Windows 11 custom windowing, Snap Layout integration, materials, global
  appearance, accents, reduced motion, high contrast, localization, DPI, static
  packaging, and migrations.

Serial, cloud, collaboration, AI, remote editing, and a standalone dual-pane
file manager remain outside V2.

## Required owner evidence

This record does not call pending manual work a pass. Follow
[DEFERRED_MANUAL_ACCEPTANCE.md](DEFERRED_MANUAL_ACCEPTANCE.md), using only the
two hashes above. The remaining categories are:

- actual Windows contrast themes, Narrator, and full keyboard review;
- English/Chinese visual routes and available physical-DPI/Snap states;
- local shell, terminal Unicode/IME/selection/alternate-screen stress;
- authorized password and `id_ed25519` SSH, host trust, SFTP, and transfer
  lifecycle checks;
- Windows Credential Manager and encrypted portable-vault workflows;
- exact MSI upgrade/uninstall, portable replacement, and clean Windows Sandbox
  launch/install;
- repeated shutdown with active terminal, log, SFTP, and transfer resources.

Until the owner performs or explicitly waives those checks, `4c92a89` is a
fully automated **V2 implementation candidate**, not an accepted public
release. No license file is added; the project owner has not selected one.
